const bookModel = require('../models/bookModel')

const deleteBook = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(418)
        console.log(error)
    }
}

module.exports = deleteBook