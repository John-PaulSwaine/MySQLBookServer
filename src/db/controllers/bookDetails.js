const bookModel = require('../models/bookModel')

const bookDetails = async (req, res) => {
    try {
        
    } catch (error) {
        res.status(418)
        console.log(error)
    }
}

module.exports = bookDetails